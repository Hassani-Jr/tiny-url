openapi: 3.0.3

info:
  title: Tiny URL Shortener API
  version: 1.0.0
  description: |
    Single-binary URL shortener with click analytics, owner-token auth, SSRF
    protection, deny-list, and a privacy-respecting click event log.

    ## Authentication

    Mutating endpoints (`PATCH /api/url/{code}`, `DELETE /api/url/{code}`)
    and analytics endpoints (`GET /api/analytics/{code}`,
    `GET /api/analytics/{code}/clicks`) require the per-URL **admin token**
    returned at creation time, supplied as `Authorization: Bearer <token>`.

    The plaintext token is shown **once** in the `POST /api/shorten`
    response. The server stores only its SHA-256 hash. Save the token at
    create time — there is no recovery mechanism.

    ## CSRF

    `POST /api/shorten` requires the `X-Requested-With: XMLHttpRequest`
    header. This blocks `<form>` / `<img>` cross-origin abuse from a
    malicious page. The header cannot be set cross-origin without a CORS
    preflight, which the server doesn't permit.

    `PATCH` and `DELETE` are gated by the bearer token and don't need this
    header (the token is unforgeable from another origin).

    ## Rate limiting

    All endpoints are subject to per-IP rate limits except `/healthz`,
    `/readyz`, `/metrics`, and `/favicon.ico`. Limits are configurable via
    `WRITE_RATE_PER_MIN` and `READ_RATE_PER_MIN`. On limit breach the
    response is `429 Too Many Requests` with a `Retry-After` header.

  contact:
    name: Tiny URL maintainer

servers:
  - url: http://localhost:8080
    description: Local dev (memory backend by default)
  - url: https://short.example.com
    description: Operator-deployed production host

tags:
  - name: URLs
    description: Create, redirect, modify, delete short URLs.
  - name: Analytics
    description: Owner-only metrics and click event log.
  - name: Keys
    description: Account-level API keys that own multiple short URLs.
  - name: Operations
    description: Liveness, readiness, metrics.

paths:
  /api/shorten:
    post:
      tags: [URLs]
      summary: Create a short URL
      description: |
        Returns a fresh short code, the canonical short URL, and the admin
        token (shown ONCE). The destination URL is validated:
        scheme must be `http`/`https`, length ≤ 2048, no embedded
        credentials, and the host must not resolve to a private/loopback/
        link-local/multicast/CGNAT address (SSRF guard).
      parameters:
        - $ref: '#/components/parameters/XRequestedWith'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ShortenRequest'
      responses:
        '201':
          description: Created
          headers:
            X-Request-ID:
              $ref: '#/components/headers/RequestID'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ShortenResponse'
        '400':
          description: Invalid request (bad scheme, SSRF blocked, oversized body, invalid custom code, …)
          content:
            application/json:
              schema: { $ref: '#/components/schemas/ErrorResponse' }
        '403':
          description: Missing X-Requested-With header
          content:
            application/json:
              schema: { $ref: '#/components/schemas/ErrorResponse' }
        '409':
          description: Custom code already in use
          content:
            application/json:
              schema: { $ref: '#/components/schemas/ErrorResponse' }
        '429': { $ref: '#/components/responses/RateLimited' }

  /api/url/{code}:
    parameters:
      - $ref: '#/components/parameters/Code'
    patch:
      tags: [URLs]
      summary: Update destination URL and/or expiration
      description: |
        Owner-token gated. At least one of `url` or `expiration_mins` must
        be provided. New destination URLs go through the same SSRF and
        deny-list checks as `POST /api/shorten`.

        | `expiration_mins`     | Effect                                           |
        |-----------------------|--------------------------------------------------|
        | omitted               | leave expiration unchanged                       |
        | `0`                   | **remove** expiration — URL becomes never-expiring |
        | `> 0`                 | set expiration to N minutes from now             |
      security:
        - BearerToken: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/PatchRequest'
      responses:
        '200':
          description: Updated
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PatchResponse'
        '400':
          description: Invalid body or destination URL rejected
          content:
            application/json:
              schema: { $ref: '#/components/schemas/ErrorResponse' }
        '401':
          description: Missing or invalid admin token
          content:
            application/json:
              schema: { $ref: '#/components/schemas/ErrorResponse' }
        '404': { description: Short code not found }
        '410': { description: URL has expired }
        '429': { $ref: '#/components/responses/RateLimited' }

    delete:
      tags: [URLs]
      summary: Delete a short URL
      description: |
        Owner-token gated. Cascades to the click event log on the SQLite
        backend (foreign key `ON DELETE CASCADE`). Returns 204 even if the
        code was already gone (idempotent).
      security:
        - BearerToken: []
      responses:
        '204': { description: Deleted }
        '401':
          description: Missing or invalid admin token
          content:
            application/json:
              schema: { $ref: '#/components/schemas/ErrorResponse' }
        '404': { description: Short code not found }
        '410': { description: URL has expired (cleanup goroutine will reap) }
        '429': { $ref: '#/components/responses/RateLimited' }

  /{code}:
    parameters:
      - $ref: '#/components/parameters/Code'
    get:
      tags: [URLs]
      summary: Redirect to the original URL
      description: |
        302 redirect. Increments the click counter and appends a click
        event atomically. The destination is re-validated at redirect time
        (deny-list + runtime SSRF check) so DNS rebinding cannot
        retroactively turn a previously-valid host into a private-network
        target.

        When the short URL has a click cap and the current count meets
        it, 410 Gone is served — the cleanup goroutine will reap the row
        on its next pass.

        When the short URL is password-protected, a self-contained HTML
        form is served (200) instead of the 302. Submitting the form via
        `POST /{code}` with the correct password completes the redirect.
      responses:
        '200':
          description: |
            Password interstitial form (HTML). Served when the URL is
            password-protected and the request has not yet submitted a
            valid password.
          content:
            text/html: {}
        '302':
          description: Redirect to original URL
          headers:
            Location:
              schema: { type: string, format: uri }
            X-Request-ID:
              $ref: '#/components/headers/RequestID'
        '404': { description: Short code does not exist }
        '410': { description: URL has expired or reached its click cap }
        '451':
          description: |
            Destination is on the deny-list, or its host now resolves to
            a private/loopback address (rebinding caught at redirect time).
        '429': { $ref: '#/components/responses/RateLimited' }
    post:
      tags: [URLs]
      summary: Submit password for a password-protected short URL
      description: |
        Form-encoded body with field `password`. On match: 302 to the
        destination (and the click is recorded as a normal click). On
        mismatch: 401 + the same interstitial form. Failed POSTs do
        not increment the click counter.
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              required: [password]
              properties:
                password: { type: string }
      responses:
        '302':
          description: Password accepted; redirect to original URL.
        '401':
          description: Password incorrect; interstitial re-rendered.
          content:
            text/html: {}
        '404': { description: Short code does not exist }
        '410': { description: URL has expired or reached its click cap }
        '429': { $ref: '#/components/responses/RateLimited' }

  /api/analytics/{code}:
    parameters:
      - $ref: '#/components/parameters/Code'
    get:
      tags: [Analytics]
      summary: Get click counter and metadata for a short URL
      security:
        - BearerToken: []
      responses:
        '200':
          description: OK
          headers:
            Cache-Control:
              schema: { type: string, default: no-store }
              description: Owner-only data; never cached.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AnalyticsResponse'
        '401':
          description: Missing or invalid admin token
          content:
            application/json:
              schema: { $ref: '#/components/schemas/ErrorResponse' }
        '404': { description: Short code not found }
        '410': { description: URL has expired }
        '429': { $ref: '#/components/responses/RateLimited' }

  /api/analytics/{code}/clicks:
    parameters:
      - $ref: '#/components/parameters/Code'
    get:
      tags: [Analytics]
      summary: Get the per-event click log
      description: |
        Returns the most recent click events, newest-first. The memory
        backend caps at 1000 events per code; the SQLite backend honors
        `CLICK_RETENTION_DAYS` (default 90). `ip_hash` is only present
        when `CLICK_LOG_IP=true` on the server.
      security:
        - BearerToken: []
      parameters:
        - in: query
          name: limit
          schema:
            type: integer
            minimum: 1
            maximum: 200
            default: 50
          description: Max events to return; clamped server-side at 200.
      responses:
        '200':
          description: OK
          headers:
            Cache-Control:
              schema: { type: string, default: no-store }
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ClicksResponse'
        '401':
          description: Missing or invalid admin token
          content:
            application/json:
              schema: { $ref: '#/components/schemas/ErrorResponse' }
        '404': { description: Short code not found }
        '410': { description: URL has expired }
        '429': { $ref: '#/components/responses/RateLimited' }

  /api/analytics/{code}/series:
    parameters:
      - $ref: '#/components/parameters/Code'
    get:
      tags: [Analytics]
      summary: Time-series click counts
      description: |
        Owner-token-gated. Returns an oldest-first array of click counts
        bucketed by minute/hour/day for the requested window. Used by the
        dashboard sparkline; lets clients render longer windows than the
        raw event log (capped at 200 events / per-process FIFO on memory)
        can support.
      security:
        - BearerToken: []
      parameters:
        - in: query
          name: bucket
          schema: { type: string, enum: [minute, hour, day], default: hour }
        - in: query
          name: range
          schema: { type: integer, minimum: 1 }
          description: |
            Number of buckets, oldest-first. Caps per resolution:
            minute≤240 (≈4h), hour≤168 (7d), day≤365 (1y).
            Defaults: 60 / 24 / 30.
      responses:
        '200':
          description: OK
          headers:
            Cache-Control:
              schema: { type: string, default: no-store }
          content:
            application/json:
              schema:
                type: object
                properties:
                  short_code:   { type: string }
                  bucket:       { type: string, enum: [minute, hour, day] }
                  bucket_secs:  { type: integer, description: Bucket width in seconds. }
                  end:          { type: string, format: date-time, description: Exclusive upper bound of the newest bucket. }
                  counts:       { type: array, items: { type: integer }, description: Oldest first. }
        '400': { description: Invalid bucket / range }
        '401': { description: Missing or invalid admin token }
        '404': { description: Short code not found }
        '410': { description: URL has expired }
        '429': { $ref: '#/components/responses/RateLimited' }

  /api/keys:
    post:
      tags: [Keys]
      summary: Mint a new API key
      description: |
        Anyone can create an API key. The returned `token` is shown
        ONCE; the server stores only its SHA-256 hash. Subsequent
        requests authenticate via `Authorization: Bearer <token>` —
        the same header used for per-URL admin tokens.

        Use cases: claim ownership of URLs you create with
        `POST /api/shorten` (the server binds the new URL to the
        authenticated key), and list/manage them across devices via
        `GET /api/urls`. Existing per-URL admin tokens keep working
        unchanged.
      parameters:
        - $ref: '#/components/parameters/XRequestedWith'
      requestBody:
        required: false
        content:
          application/json:
            schema:
              type: object
              properties:
                label:
                  type: string
                  maxLength: 64
                  description: Optional owner-supplied label.
      responses:
        '201':
          description: Created
          content:
            application/json:
              schema:
                type: object
                required: [id, token, created_at]
                properties:
                  id:         { type: integer, format: int64 }
                  label:      { type: string }
                  created_at: { type: string, format: date-time }
                  token:      { type: string, description: 'Shown once. Save it deliberately.' }
        '400': { description: Body too large or malformed }
        '403': { description: Missing X-Requested-With header }
        '429': { $ref: '#/components/responses/RateLimited' }
    get:
      tags: [Keys]
      summary: Get the calling key's metadata
      security:
        - BearerToken: []
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                type: object
                properties:
                  id:           { type: integer, format: int64 }
                  label:        { type: string }
                  created_at:   { type: string, format: date-time }
                  last_used_at: { type: string, format: date-time, nullable: true }
        '401': { description: Missing or invalid API key }
    patch:
      tags: [Keys]
      summary: Update the calling key's label
      security:
        - BearerToken: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [label]
              properties:
                label: { type: string, maxLength: 64 }
      responses:
        '200':
          description: Updated
        '400': { description: Invalid body }
        '401': { description: Missing or invalid API key }
    delete:
      tags: [Keys]
      summary: Revoke the calling key
      description: |
        Revokes the key used to authenticate this request. URLs that
        were bound to the key SURVIVE — their `api_key_id` column is
        cleared, and the per-URL admin token remains a valid
        credential. To delete those URLs as well, call `DELETE
        /api/url/{code}` for each before revoking the key.
      security:
        - BearerToken: []
      responses:
        '204': { description: Revoked }
        '401': { description: Missing or invalid API key }

  /api/urls:
    get:
      tags: [Keys]
      summary: List URLs owned by the calling key
      description: |
        Paginated, newest-first. Each entry has the same shape as
        `AnalyticsResponse` so the dashboard can render the table
        without per-row analytics fetches.
      security:
        - BearerToken: []
      parameters:
        - in: query
          name: limit
          schema: { type: integer, minimum: 1, maximum: 100, default: 50 }
        - in: query
          name: offset
          schema: { type: integer, minimum: 0, default: 0 }
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                type: object
                properties:
                  urls:
                    type: array
                    items: { $ref: '#/components/schemas/AnalyticsResponse' }
        '401': { description: Missing or invalid API key }
        '429': { $ref: '#/components/responses/RateLimited' }

  /api/url/{code}/rotate:
    parameters:
      - $ref: '#/components/parameters/Code'
    post:
      tags: [URLs]
      summary: Rotate the admin token
      description: |
        Issues a fresh admin token and atomically invalidates the old one.
        Useful when a token is suspected compromised. The OLD token must
        be presented in `Authorization` so the rotation itself is gated
        the same way as analytics/PATCH/DELETE.
      security:
        - BearerToken: []
      responses:
        '200':
          description: Token rotated
          content:
            application/json:
              schema:
                type: object
                properties:
                  short_code:  { type: string }
                  admin_token: { type: string, description: New token. Save it — the old one no longer works. }
        '401':
          description: Missing or invalid (current) admin token
          content:
            application/json:
              schema: { $ref: '#/components/schemas/ErrorResponse' }
        '404': { description: Short code not found }
        '410': { description: URL has expired }
        '429': { $ref: '#/components/responses/RateLimited' }

  /api/analytics/{code}/stream:
    parameters:
      - $ref: '#/components/parameters/Code'
    get:
      tags: [Analytics]
      summary: Live click event stream (Server-Sent Events)
      description: |
        Owner-token-gated SSE endpoint. Pushes one `event: click` per click
        as it happens, plus a `:heartbeat` comment every 15 seconds to keep
        proxies / browsers from idle-closing.

        The browser `EventSource` API can't set Authorization headers, so
        the dashboard uses `fetch()` with a streaming body reader. Both
        approaches work against this endpoint unchanged — it just emits
        standard SSE.

        SSE is best-effort presentation. Slow consumers drop events
        rather than apply backpressure on the redirect path. The
        canonical record is the `click_events` table (queryable via
        `GET /api/analytics/{code}/clicks`).
      security:
        - BearerToken: []
      responses:
        '200':
          description: SSE stream
          content:
            text/event-stream:
              schema:
                type: string
                example: |
                  : connected

                  event: click
                  data: {"at":"2026-05-10T12:34:56Z","ua_class":"desktop"}

                  : heartbeat

        '401':
          description: Missing or invalid admin token
        '404': { description: Short code not found }
        '410': { description: URL has expired }
        '429': { $ref: '#/components/responses/RateLimited' }

  /api/badge/{code}:
    parameters:
      - $ref: '#/components/parameters/Code'
    get:
      tags: [URLs]
      summary: Get an embeddable click-count SVG badge
      description: |
        Unauthenticated. Returns a shields.io-shaped SVG showing the
        current click count for a short code. Designed to be embedded as
        `<img src="https://your-host/api/badge/{code}.svg">` on blogs or
        READMEs. Cached publicly for 60 seconds.

        Intended for codes the owner has chosen to publicise. Anyone who
        knows the short code can read its click count via this endpoint.
      responses:
        '200':
          description: SVG badge
          headers:
            Cache-Control:
              schema: { type: string, default: 'public, max-age=60' }
          content:
            image/svg+xml:
              schema:
                type: string
                format: binary
        '404': { description: Short code does not exist }
        '410': { description: URL has expired }
        '429': { $ref: '#/components/responses/RateLimited' }

  /api/qr/{code}:
    parameters:
      - $ref: '#/components/parameters/Code'
    get:
      tags: [URLs]
      summary: Get a PNG QR code encoding the short URL
      description: |
        Unauthenticated by design — the encoded value is the same short URL
        anyone with the code can already visit, so a QR adds no privilege.
      responses:
        '200':
          description: PNG image
          headers:
            Cache-Control:
              schema: { type: string, default: 'public, max-age=86400, immutable' }
          content:
            image/png:
              schema:
                type: string
                format: binary
        '404': { description: Short code does not exist }
        '410': { description: URL has expired }
        '429': { $ref: '#/components/responses/RateLimited' }

  /healthz:
    get:
      tags: [Operations]
      summary: Liveness probe
      description: |
        Cheap, no-IO. Always 200 on a running process. Use as a Kubernetes
        `livenessProbe`. Successful (`<400`) hits are deliberately not
        logged to avoid drowning real request logs.
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:  { type: string, example: ok }
                  storage: { type: string, example: memory }

  /readyz:
    get:
      tags: [Operations]
      summary: Readiness probe (storage-aware)
      description: |
        Pings the storage backend with a 2-second timeout. Use as a
        Kubernetes `readinessProbe` so a database outage drains traffic
        at the load balancer instead of being absorbed silently.
      responses:
        '200':
          description: Ready
          content:
            application/json:
              schema:
                type: object
                properties:
                  status: { type: string, example: ready }
        '503':
          description: Storage backend not reachable
          content:
            application/json:
              schema:
                type: object
                properties:
                  status: { type: string, example: not_ready }
                  error:  { type: string }

  /api/audit:
    get:
      tags: [Operations]
      summary: Audit log (operator-only)
      description: |
        Returns state-changing actions newest-first. Each event has an
        actor (who did it), an action (what they did), and a target
        (what they did it to). Actions covered today: `url.create`,
        `url.delete`, `url.patch`, `url.token_rotated`,
        `api_key.create`, `api_key.revoke`, `api_key.label_updated`.

        Gated by `METRICS_TOKEN` (same credential as `/metrics`). When
        `METRICS_TOKEN` is unset the endpoint is open — same posture
        as the existing metrics endpoint for installs on a private
        network.
      security:
        - BearerToken: []
      parameters:
        - in: query
          name: limit
          schema: { type: integer, minimum: 1, maximum: 500, default: 50 }
        - in: query
          name: offset
          schema: { type: integer, minimum: 0, default: 0 }
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                type: object
                properties:
                  events:
                    type: array
                    items:
                      type: object
                      properties:
                        id:          { type: integer, format: int64 }
                        at:          { type: string, format: date-time }
                        actor_kind:  { type: string, enum: [apikey, admin_token, anon, system] }
                        actor_id:    { type: string }
                        action:      { type: string }
                        target_kind: { type: string, enum: [url, api_key] }
                        target_id:   { type: string }
                        metadata:    { type: string, description: 'JSON; opaque' }
                        request_id:  { type: string }
        '401':
          description: METRICS_TOKEN is configured and the request lacked or mismatched the token

  /metrics:
    get:
      tags: [Operations]
      summary: Expvar metrics exposition
      description: |
        Standard Go `expvar` JSON. Includes `http_requests_total` (status
        class), `http_requests_route_total` (route + status class),
        `http_requests_in_flight`, `panics_total`, plus runtime metrics.

        When `METRICS_TOKEN` is set, this endpoint requires
        `Authorization: Bearer <token>`; otherwise it is open.
      responses:
        '200':
          description: expvar JSON exposition
          content:
            application/json: {}
        '401':
          description: METRICS_TOKEN is configured and the request lacked or mismatched the token

components:

  parameters:
    Code:
      in: path
      name: code
      required: true
      schema:
        type: string
        pattern: '^[A-Za-z0-9_-]{3,32}$'
      description: 3-32 chars of `[A-Za-z0-9_-]`. Random base62 by default; user-chosen via `custom_code`.
    XRequestedWith:
      in: header
      name: X-Requested-With
      required: true
      schema:
        type: string
        enum: [XMLHttpRequest]
      description: CSRF defense for `POST /api/shorten`.

  headers:
    RequestID:
      schema: { type: string }
      description: |
        Per-request correlation ID. Honors inbound `X-Request-ID`
        (≤ 64 chars) or generates a 16-hex-char ID. Echoed in every
        structured log line for the request.

  responses:
    RateLimited:
      description: Per-IP rate limit exceeded
      headers:
        Retry-After:
          schema: { type: integer }
          description: Seconds until the bucket refills.

  securitySchemes:
    BearerToken:
      type: http
      scheme: bearer
      bearerFormat: '32-byte URL-safe-base64 token'
      description: |
        Bearer token. Accepts EITHER:

        - Per-URL admin token returned at create time (covers analytics,
          PATCH, DELETE, rotate, clicks, series, and stream for that
          specific URL), OR
        - Account-level API key from `POST /api/keys` (covers every URL
          bound to that key via `api_key_id`, plus the /api/keys and
          /api/urls endpoints).

        Both are compared in constant time against the stored SHA-256
        hash. A bearer that matches the URL's admin token is preferred —
        the API key lookup only runs when the admin-token check fails
        AND the URL has a non-zero `api_key_id`.

  schemas:

    ShortenRequest:
      type: object
      required: [url]
      properties:
        url:
          type: string
          format: uri
          maxLength: 2048
          description: Destination. Must be `http://` or `https://` and resolve to a public IP.
          example: https://example.com/some/long/path
        custom_code:
          type: string
          pattern: '^[A-Za-z0-9_-]{3,32}$'
          description: Optional user-chosen alias. Must be unique.
          example: my-link
        expiration_mins:
          type: integer
          minimum: 0
          description: Optional expiration. `0` or omitted → never expires. Capped at `MAX_EXPIRATION_MINUTES`.
        tags:
          type: array
          items: { type: string, maxLength: 32 }
          maxItems: 16
          description: Owner-supplied labels. Server normalizes (trims, dedupes) and rejects entries over 32 chars or lists over 16 entries.
          example: ["work", "campaign-spring"]
        max_clicks:
          type: integer
          minimum: 0
          description: Cap on total redirects. `0` or omitted → unlimited. Once the count is reached the redirect returns 410 Gone.
        password:
          type: string
          maxLength: 256
          description: Optional passphrase. When set, the short URL serves an interstitial HTML form before redirecting. Stored as PBKDF2-SHA256 hash + salt.
        webhook_url:
          type: string
          format: uri
          description: |
            Optional HTTPS endpoint POST'd on each successful click. Same
            SSRF / deny-list rules as the destination URL. A 32-byte
            random HMAC-SHA256 key is generated server-side and returned
            ONCE in `webhook_secret` — receivers verify the
            `X-Tinyurl-Signature: sha256=<hex>` header against the raw
            request body. 5xx responses are retried (3 attempts, 200/800
            ms backoff); 4xx is treated as terminal misconfiguration.

    ShortenResponse:
      type: object
      required: [short_code, short_url, original_url, admin_token]
      properties:
        short_code:
          type: string
          example: aBc123
        short_url:
          type: string
          format: uri
          example: http://localhost:8080/aBc123
        original_url:
          type: string
          format: uri
        admin_token:
          type: string
          description: |
            **Shown once.** Required for analytics, edit, and delete on
            this short code. Save it deliberately.
          example: 7XQ-very-long-base64-token-shown-only-here
        expires_at:
          type: string
          format: date-time
          nullable: true
        tags:
          type: array
          items: { type: string }
          description: Canonicalized list as stored on the server.
        max_clicks:
          type: integer
        has_password:
          type: boolean
        webhook_url:
          type: string
          format: uri
          description: Empty when no webhook is configured.
        webhook_secret:
          type: string
          description: Returned ONCE when a webhook is configured at create time. base64url-encoded 32 random bytes; HMAC-SHA256 key.

    AnalyticsResponse:
      type: object
      properties:
        short_code:    { type: string }
        original_url:  { type: string, format: uri }
        click_count:   { type: integer }
        created_at:    { type: string, format: date-time }
        expires_at:    { type: string, format: date-time, nullable: true }
        last_accessed: { type: string, format: date-time, nullable: true }
        tags:          { type: array, items: { type: string } }
        max_clicks:    { type: integer }
        has_password:  { type: boolean }
        webhook_url:   { type: string, format: uri }

    ClicksResponse:
      type: object
      properties:
        short_code: { type: string }
        count:      { type: integer, description: Number of events returned (≤ limit). }
        events:
          type: array
          description: Events newest-first.
          items: { $ref: '#/components/schemas/ClickEvent' }

    ClickEvent:
      type: object
      properties:
        at:
          type: string
          format: date-time
          description: When the click occurred.
        ip_hash:
          type: string
          description: Hex-encoded SHA-256 of `salt || ip`. Present only when `CLICK_LOG_IP=true`.
        referer:
          type: string
          description: Truncated to 512 chars.
        ua_class:
          type: string
          enum: [bot, mobile, tablet, desktop, unknown]
          description: |
            Coarse User-Agent bucket. The raw UA is intentionally not
            stored — it would be a fingerprinting vector and a targeting
            hint for exploit kits.
        country:
          type: string
          description: |
            ISO-3166-1 alpha-2 country code resolved from the client IP
            via the embedded MaxMind GeoLite2 database. Empty when
            geoip is disabled, the IP is private, or no match exists.

    PatchRequest:
      type: object
      description: At least one of `url`, `expiration_mins`, `tags`, `max_clicks`, or `password` must be present.
      properties:
        url:
          type: string
          format: uri
          description: New destination. Re-validated against SSRF + deny-list.
        expiration_mins:
          type: integer
          minimum: 0
          description: '`0` removes expiration; `>0` sets new expiry from now; omitted → unchanged.'
        tags:
          type: array
          items: { type: string, maxLength: 32 }
          maxItems: 16
          description: '`[]` clears all tags; non-empty replaces the whole list; omitted → unchanged.'
        max_clicks:
          type: integer
          minimum: 0
          description: '`0` removes the cap; `>0` sets a new cap (must exceed current click_count); omitted → unchanged.'
        password:
          type: string
          maxLength: 256
          description: '`""` clears the password; non-empty sets/replaces it; omitted → unchanged.'
        webhook_url:
          type: string
          description: '`""` clears the webhook; non-empty sets/replaces it; omitted → unchanged. Newly set or rotated webhooks return a fresh `webhook_secret` in the response.'
        webhook_rotate_secret:
          type: boolean
          description: When `true`, issues a fresh webhook secret (returned once) without changing the URL. Requires an existing webhook.

    PatchResponse:
      type: object
      properties:
        short_code:   { type: string }
        original_url: { type: string, format: uri }
        expires_at:   { type: string, format: date-time, nullable: true }

    ErrorResponse:
      type: object
      properties:
        error:   { type: string, example: 'Bad Request' }
        message: { type: string, example: 'URL host points at a private or reserved network' }
